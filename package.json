{
  "name": "marinellp-monorepo",
  "version": "1.0.0",
  "description": "Marine LLP Web Application",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "build:frontend": "npm install --prefix apps/frontend && npm run build --prefix apps/frontend",
    "install:frontend": "npm install --prefix apps/frontend",
    "start": "npm run start --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vercel": "^50.1.3"
  }
}
